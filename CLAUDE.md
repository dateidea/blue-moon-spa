# CLAUDE.md, Vivi Worktree

## Critical: read the Brain vault first

This is one of Mohammad's projects. Before doing anything else, read his Brain vault for context:

1. **Read first:** `~/Brain/_Meta/CLAUDE.md` for vault navigation rules and YAML schema.
2. **Read this project note:** `~/Brain/Money/Projects/Vivi-Worktree.md`.
3. **Read recent sessions:** glob `~/Brain/Sessions/` and read the last 1 to 3 notes that link to `[[Vivi-Worktree]]`.
4. **Read related Resources** if relevant (linked in the project note).

The vault is Mohammad's external memory. Read CLAUDE.md first, then load only what you need. Do not pre-load the whole vault.

## Project facts

- **Pillar:** `money`
- **Project folder:** `/Users/qudsismacfolder/Brain/Money/source/vivi-worktree`
- **Vault project note:** `~/Brain/Money/Projects/Vivi-Worktree.md`

## Style rules

- Never use em or en dashes. Use periods, commas, or sentence breaks.
- Keep notes short. The vault is a memory aid, not a manuscript.
- Link liberally with `[[wikilinks]]` when writing in the vault.

## End of session

At the end of every session, write a session note in the vault:
- Path: `~/Brain/Sessions/<descriptive title>.md`
- Schema: see `~/Brain/_Templates/Session.md`
- Set `pillar: money`
- Link to `[[Vivi-Worktree]]` in the `project:` field
- Fill all sections: Context, What we did, Key takeaways, Open questions, Next actions

When Mohammad makes a decision worth remembering, write a Decision note in `~/Brain/Decisions/`.
When Mohammad has an idea worth saving, write an Idea note in `~/Brain/Ideas/`.
When new tasks come up, write Task notes in `~/Brain/Tasks/`.
